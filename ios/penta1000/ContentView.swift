import SwiftUI

struct ContentView: View {
    var body: some View {
        Image("penta1000").background(Color.black)
    }
}

struct ContentView_Previews: PreviewProvider {
    static var previews: some View {
        ContentView()
    }
}
